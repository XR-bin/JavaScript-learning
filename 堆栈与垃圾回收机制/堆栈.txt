栈区(stack)：由编译器自动分配释放，存放函数的参数值，局部变量的值等。
堆区(heap)：一般由程序员分配释放，若程序员不释放，程序结束时候可能由OS回收

基本数据类型值指的是保存在栈内存中的简单数据段。访问方式是按值访问。

引用数据类型值指的是保存在堆内存指的对象，也就是，变量的栈内存中保存的实际上
只是一个指针(或者说是地址),这个指针指向堆内存中的一个位置，该位置存放的就是对
象的数据。访问方式是按引用访问